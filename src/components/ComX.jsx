import React from "react"

export default function ComX(props) {
    return (
        <div>
            ComX
            {props.children}
            {/* <div>{props.products}</div> */}
        </div>
    )
}
